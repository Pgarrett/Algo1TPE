\documentclass[a4paper]{article}

\usepackage[spanish]{babel} % Le indicamos a LaTeX que vamos a escribir en espa�ol.
\usepackage[latin1]{inputenc} % Permite utilizar tildes y e�es normalmente
%\usepackage{framed}
\input{Algo1Macros}% Macros especificas para especificar problemas en AyEDI

\newcommand{\comen}[2]{%
\begin{framed}
\noindent \textsf{#1:} #2
\end{framed}
}
% Aca solo vamos a poner el esqueleto del documento, pero no vamos a especificar nada.

\begin{document} % Todo lo que escribamos a partir de aca va a aparecer en el documento.

\section{Tipos}

\input{tipos/tipos}

\section{Drone}

\input{tipos/drone}

\begin{problema}{vueloEscaladeroD}{d: Drone}{\bool}
\asegura{enVuelo(d)}
% Habria que revisar aca, para mi que faltan unos casos
\asegura[subeBajaONada]{subeEscalera(vueloRealizado(d)) \lor bajaEscalera(vueloRealizado(d))}
\end{problema}

\noindent\aux{subeEscalera}{xs: [(\ent,\ent)]}{\bool}{
(\forall i \selec [0..|xs|-1)) (prm(xs_{i}) == prm(xs_{i+1}) \land sgd(xs_{i}) + 1 == sgd(xs_{i+1})) \lor (prm(xs_{i}) + 1 == prm(xs_{i+1}) \land sgd(xs_{i}) == sgd(xs_{i+1}))
}

%\section{Funciones Auxiliares}

%\input{espec/auxiliares.tex}

\end{document} %Termin�!
