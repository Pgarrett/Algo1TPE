\documentclass[a4paper]{article}

\usepackage[spanish]{babel} % Le indicamos a LaTeX que vamos a escribir en espa�ol.
\usepackage[latin1]{inputenc} % Permite utilizar tildes y e�es normalmente
%\usepackage{framed}
\input{Algo1Macros}% Macros especificas para especificar problemas en AyEDI

\newcommand{\comen}[2]{%
\begin{framed}
\noindent \textsf{#1:} #2
\end{framed}
}
% Aca solo vamos a poner el esqueleto del documento, pero no vamos a especificar nada.

\begin{document} % Todo lo que escribamos a partir de aca va a aparecer en el documento.

\section{Tipos}

\input{tipos/tipos}

\section{Drone}

\input{tipos/drone}

\begin{problema}{vueloEscaladeroD}{d: Drone}{\bool}
%Aca vamos a tener que aclarar que asumimos que una escalera sin escalones no es escalera, le pregunte a un ayudante y me dijo que pongamos un comentario.%
\asegura{ res == |vueloRealizado(d)| \geq 3 \land esEscalera(vueloRealizado(d))}
\end{problema}

\noindent \aux{esEscalera}{ps: [(\ent, \ent)]}{Bool}{primerosTresSonEscalera(ps) \land (\forall \selector {i}{\rangoca{0}{|ps|-1}}) comoLosPrimerosTres(ps, i)}
\noindent \aux{primerosTresSonEscalera}{ps: [(\ent, \ent)]}{\bool}{|\prm{restarPos(ps_{2}, ps_{0})}| == 1 \land |\sgd{restarPos(ps_{2}, ps_{0})}| == 1}
\noindent \aux{comoLosPrimerosTres}{ps: [(\ent, \ent)], i: \ent}{\bool}{restarPos(ps_{i+1}, ps_{i}) == restarPos(ps_{(i \bmod 2) + 1}, ps_{i \bmod 2})}
\noindent \aux{restarPos}{a,b: (\ent, \ent)}{(\ent, \ent)}{ (\prm{a}-\prm{b}, \sgd{a}-\sgd{b})}

%\section{Funciones Auxiliares}

%\input{espec/auxiliares.tex}

\end{document} %Termin�!
