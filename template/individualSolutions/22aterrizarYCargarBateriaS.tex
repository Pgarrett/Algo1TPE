\documentclass[a4paper]{article}

\usepackage[spanish]{babel} % Le indicamos a LaTeX que vamos a escribir en espa�ol.
\usepackage[latin1]{inputenc} % Permite utilizar tildes y e�es normalmente
%\usepackage{framed}
\input{Algo1Macros}% Macros especificas para especificar problemas en AyEDI

\newcommand{\comen}[2]{%
\begin{framed}
\noindent \textsf{#1:} #2
\end{framed}
}
% Aca solo vamos a poner el esqueleto del documento, pero no vamos a especificar nada.

\begin{document} % Todo lo que escribamos a partir de aca va a aparecer en el documento.

\section{Tipos}

\input{tipos/tipos}

\section{Sistema}

\input{tipos/sistema}

\begin{problema}{aterrizarYCargarBateriaS}{s: Sistema, b: \ent}{}

\modifica{s}
\asegura[mismoCampo]{campo(s) == campo(pre(s))}
\asegura[mismoEstado]{(\forall {p} \selec {parcelasConCultivo(campo(s))}) \\ estadoDelCultivo(s, prm(p), sgd(p)) == estadoDelCultivo(pre(s), prm(p), sgd(p))}

\asegura[mismosDronesYProductos]{mismosDronesYProductos(enjambreDrones(s), enjambreDrones(pre(s))}
\asegura[dronesEnVueloTenianBateria]{(\forall{d} \selec {enjambreDrones(s)}, d' \selec{enjambeDrones(pre(s))}, id(d) == id(d')) enVuelo(d) \Rightarrow (bateria(d') > b)}
\asegura[dronesConBateriaEstanIgual]{(\forall{d} \selec {enjambreDrones(s)}, d' \selec{enjambeDrones(pre(s))}, id(d) == id(d')) (bateria(d')) \ge b) \Rightarrow mismoVueloYBateria(d, d')}

\end{problema}

%\section{Funciones Auxiliares}
\noindent \aux{mismosDronesYProductos}{ds1, ds2: [Drone]}{\bool}{(|ds1| == |ds2|) \land (sinRepetidos(d1))) \land ((\forall d1 \selec ds1) \exists d2 \in ds2 | (id(d1) == id(d2)) \land (mismos(productosDisponibles(d1),  productosDisponibles(d2))))}
\noindent \aux{mismoVueloYBateria}{d, d': Drone}{\bool}{(bateria(d) == bateria(d')) \land (vueloRealizado(d) == vueloRealizado(d')) \land (posicionActual(d) == posicionActual(d'))}


%\section{Funciones Auxiliares}

%\input{espec/auxiliares.tex}

\end{document} %Termin�!
